# Copy this file to .env (local) or use these keys in Render environment settings.

# Database (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# Clerk (App Router)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="YOUR_PUBLISHABLE_KEY"
CLERK_SECRET_KEY="YOUR_SECRET_KEY"
SITE_URL="http://localhost:3000"  # Set to your production URL in prod env

# Optional: comma-separated admin allowlist for auto-promotion on first sign-in
# Example: ADMIN_EMAILS="admin@example.com,other.admin@example.com"
ADMIN_EMAILS=""

# Cloudinary (required if using /api/upload/sign for direct uploads)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
