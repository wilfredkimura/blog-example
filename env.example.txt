# Copy this file to .env (local) or use these keys in Render environment settings.

# Database (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# NextAuth core
NEXTAUTH_URL="http://localhost:3000"           # Set to your production URL on Render
NEXTAUTH_SECRET="set-a-strong-random-secret"   # Required in production
# Optional: promote specific emails to ADMIN on first login (comma-separated)
ADMIN_EMAILS="admin@example.com"

# OAuth providers (optional; enable if you want these buttons on the sign-in page)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
TWITTER_CLIENT_ID=""
TWITTER_CLIENT_SECRET=""

# Cloudinary (required if using /api/upload/sign for direct uploads)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
